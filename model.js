export function distanceKm(a,b){const rad=x=>x*Math.PI/180;const dLat=rad(b.lat-a.lat),dLon=rad(b.lon-a.lon);const q=Math.sin(dLat/2)**2+Math.cos(rad(a.lat))*Math.cos(rad(b.lat))*Math.sin(dLon/2)**2;return 6371*2*Math.atan2(Math.sqrt(q),Math.sqrt(Math.max(0,1-q)));}
export function activeDay(now=new Date()){const parts=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Shanghai',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(now);const get=t=>parts.find(p=>p.type===t).value;const today=Date.UTC(+get('year'),+get('month')-1,+get('day'));const day=Math.floor((today-Date.UTC(2026,8,19))/86400000)+1;return day>=1&&day<=8?day:0;}
export function selectPoints(data,state){let items=data.points.map(p=>({...p,location:p.location||data.anchors[p.anchor]}));if(state.category!=='all')items=items.filter(p=>p.category===state.category);if(state.range==='route'){if(state.day)items=items.filter(p=>p.days.includes(state.day));}else{if(!state.position)return [];items=items.filter(p=>distanceKm(state.position,p.location)<=Number(state.range));}if(state.position)items=items.map(p=>({...p,distance:distanceKm(state.position,p.location)})).sort((a,b)=>a.distance-b.distance);return items;}
